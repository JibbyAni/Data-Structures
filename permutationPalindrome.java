bool isPalindromePermutation(String s){
  char[] word = s.toCharArray();
  HashSet unpairedLetters = new HashSet();
  if (int i = 0; i < word.length; i++){
    if set.contains(word[i]) { //is odd
      set.remove(word[i])
    }
    else {
      set.add(word[i])
    }
  }
  return set.size() < 2 
  }; 
